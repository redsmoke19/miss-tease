extends ../components/base/layout

block variables
  - const title = "Возврат" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "contacts.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

//- block data

block main
  main
    +contacts()
    +inst-block()

block modals
  +modal("modal--cart")(data-modal="cart")
    +modal-cart({items: modalCartItems})
