extends ../components/base/layout

block variables
  - const title = "О компании" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "about.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block data
  include ../data/modal-cart-items

block main
  main
    h1.visually-hidden О нас
    +about-hero()
    +about-company()
    +main-categories({short: true})

block modals
  +modal("modal--cart")(data-modal="cart")
    +modal-cart({items: modalCartItems})
