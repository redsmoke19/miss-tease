extends ../components/base/layout

block variables
  - const title = "Карточка комплекта"
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "set-card.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block data
  //- Тут берем переменную showMore
  include ../components/data/show-more
  //- Тут берем переменную showMoreLow
  include ../components/data/show-more-low

block main
  main
    h1.visually-hidden Карточка комплекта
    +back-link("Назад к коллекциям")
    +set-card()
    +show-more("Комплекты в других цветах", showMoreLow)
    +show-more("Вам может понравится", showMore)

block modals
  +modal("modal--cart")(data-modal="cart")
    +modal-cart({empty: true})
  +modal("modal--gallery")(data-modal="gallery")
    +modal-gallery()
  +modal("modal--sizes")(data-modal="sizes")
    +sizes({mod: 'modal'})
