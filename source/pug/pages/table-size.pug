extends ../components/base/layout

block variables
  - const title = "Главная" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "table-size.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

//- block data

block main
  main
    +table-size()

block modals
  +modal("modal--cart")(data-modal="cart")
    +modal-cart({items: modalCartItems})
